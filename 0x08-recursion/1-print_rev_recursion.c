#include "main.h"

/**
 * _print_rev_recursion - a C function that prints a string reverse
 *
 * @s: string input
*/

void _print_rev_recursion(char *s)
{
	if (*s > '\0')
	}
		_print_rev_recursion(s + 1);
		_putchar(*s);
	}
}
